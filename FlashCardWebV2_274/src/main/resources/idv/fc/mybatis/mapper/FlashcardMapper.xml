<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="idv.fc.dao.FlashcardDao">

	<select id="queryAll" resultType="Flashcard">
		select * from flashcard
	</select>

	<insert id="create" parameterType="Flashcard">
		insert into flashcard (id,term,definition)values(#{id},#{term},#{definition});
	</insert>



</mapper>